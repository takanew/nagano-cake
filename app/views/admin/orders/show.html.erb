<h1>注文詳細</h1>

<p><strong>注文ID:</strong> <%= @order.id %></p>
<p><strong>注文日時:</strong> <%= @order.created_at.strftime('%Y/%m/%d %H:%M') %></p>

<table>
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価（税込）</th>
      <th>数量</th>
      <!-- 他のカラム... -->
    </tr>
  </thead>

  <tbody>
    <% @order.order_items.each do |item| %>
      <tr>
        <td><%= item.product.name %></td>
        <td><%= item.price_with_tax %>円</td>
        <td><%= item.quantity %>個</td>
        <!-- 他のカラム... -->
      </tr>
    <% end %>
  </tbody>
</table>

<!-- 合計金額等、その他情報を表示 -->